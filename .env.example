# Postgres connection string.
# Local dev (Docker): postgresql://postgres:postgres@127.0.0.1:5434/app_db
# Neon: copy the "Pooled connection" string from the Neon dashboard (keep ?sslmode=require)
DATABASE_URL=

# Any long random string. Used to sign auth JWTs.
JWT_SECRET=
